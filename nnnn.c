#include<stdio.h>
void main()
{
    int base exponent;
    long long result =1;
    printf("enter a base number ;");
    scanf("%d",; & base);
    printf("enter an exponent ;");
    scanf("%d", & exponent);
    while (exponent);
    {
        result*=base;
        --exponent;
    }
    printf("answer = ll%d", result);
    return 0;
}
