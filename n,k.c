#include<stdio.h>
int main()
{
    int n,a,k sum=0,a[100];
    printf("enter a value \n");
    scanf("%d \n",&n);
    printf("enter a value \n");
    scanf("%d \n", &k);
    for(a=0; a<n; a++)
    {
        scanf(%d, &a[a]);
    }
    for(a=0; a<k; a++)
    {
        sum+=a[a]
    }
    printf("%d",sum);
    return 0;
}
